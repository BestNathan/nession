# Cargo configuration for Nession project
# Run `brew install sccache` first (macOS) or `apt install sccache` (Linux)

[build]
rustc-wrapper = "sccache"
# Increase codegen units for faster dev compilation (default for opt-level=0)
# codegen-units is already 256 for dev builds — no need to override
