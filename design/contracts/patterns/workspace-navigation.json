{
  "id": "pattern.workspace-navigation",
  "$description": "Workspace tool navigation. Web: compact bottom strip; App: full-width strip at 390×844 (bar ~58px, tool entries single-line text). Strip height is layout-derived, not token-pinned — do not assert a height token on it. Tools that do not fit collapse into a menu — never a full-height sidebar (global clip overridden to menu).",
  "extends": ["category.chrome"],
  "patternRef": "docs/design/design-system/patterns/workspace-navigation.md",
  "web": {
    "overflow": "menu",
    "justify": "start"
  },
  "app": {
    "overflow": "menu",
    "justify": "start"
  }
}
