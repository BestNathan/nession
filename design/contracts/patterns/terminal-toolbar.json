{
  "id": "pattern.terminal-toolbar",
  "$description": "Terminal capsule control strip (prose: terminal-capsule.md — contract id predates the prose rename). Chrome band of controls: single line, control.md density, overflow collapses into a popover (web) / sheet (app).",
  "extends": ["category.chrome", "category.control"],
  "patternRef": "docs/design/design-system/patterns/terminal-capsule.md",
  "web": {
    "justify": "space-between"
  },
  "app": {
    "justify": "space-between"
  }
}
