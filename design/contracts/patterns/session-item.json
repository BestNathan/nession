{
  "id": "pattern.session-item",
  "$description": "One session row in the list. Shipped rows are two-line summary stacks (name + metadata, optional agent copy) whose height is layout-derived — measured 63px on fixture rows, not a fixed token row. Wrapping inside the row is allowed; content must never overflow the row bounds. App rows must meet the touch target.",
  "patternRef": "docs/design/design-system/patterns/session-item.md",
  "web": {
    "wrap": true,
    "overflow": "clip"
  },
  "app": {
    "wrap": true,
    "overflow": "clip",
    "touchTargetToken": "experience.app.touchTarget.min"
  }
}
