{
  "id": "pattern.session-header",
  "$description": "Session page header. Session identity and the non-gesture navigation entry points are permanent; infrastructure context (agent/location, session lifecycle, attachment, server connection) stays quiet while healthy and appears as it degrades. The band rules below (single line, middle align, App touch band) apply to whatever the header does render — permanent healthy metadata is not part of this contract.",
  "extends": ["category.chrome"],
  "patternRef": "docs/design/design-system/patterns/session-header.md",
  "web": {
    "justify": "space-between"
  },
  "app": {
    "justify": "space-between"
  }
}
