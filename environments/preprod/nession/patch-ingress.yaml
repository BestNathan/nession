# Ingress hosts for preprod. Backend names carry no suffix — each
# environment runs in its own namespace with base names.
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nession-server
  annotations:
    higress.io/domain: "preprod.nession.nhome.local preprod.nession.bestnathan.top"
spec:
  rules:
    - host: preprod.nession.nhome.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: nession-server
                port:
                  number: 10080
    - host: preprod.nession.bestnathan.top
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: nession-server
                port:
                  number: 10080
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nession-agent
  annotations:
    higress.io/domain: "preprod.agent.nession.nhome.local preprod.agent.nession.bestnathan.top"
spec:
  rules:
    - host: preprod.agent.nession.nhome.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: nession-agent
                port:
                  number: 10080
    - host: preprod.agent.nession.bestnathan.top
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: nession-agent
                port:
                  number: 10080
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nession-ui
  annotations:
    higress.io/domain: "preprod.ui.nession.nhome.local preprod.ui.nession.bestnathan.top"
spec:
  rules:
    - host: preprod.ui.nession.nhome.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: nession-ui
                port:
                  number: 80
    - host: preprod.ui.nession.bestnathan.top
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: nession-ui
                port:
                  number: 80
