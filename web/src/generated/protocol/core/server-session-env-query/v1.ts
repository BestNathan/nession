// Generated by `just codegen` — do not edit.
//
// Source of truth: the Rust contracts in `crates/nession-git/src/protocol/`,
// `crates/nession-claude-code/src/protocol/` and `crates/nession-protocol/`.
// This file is server.session.env.query / v1.

/** The canonical protocol id. Not the wire type. */
export const PROTOCOL = 'server.session.env.query';
/** Every transport projection this contract travels as. */
export const WIRES = ['server.session.env.query'] as const;
/**
 * The only projection this contract travels as.
 *
 * Absent, deliberately, on a contract served over more than one transport: it
 * has no single wire, and a caller that needs one has to say which it means.
 * `WIRES` is always there.
 */
export const WIRE = 'server.session.env.query';
/** The contract version these shapes are. */
export const VERSION = 1;

// ── Shapes ──

export type ClientSessionEnvQueryPayload = { session_id: string, };
export type SessionEnvQueryResponse = { 
/**
 * **Names**, not references — the handler maps the agent's array through
 * `as_str`, so this wire carries `["staging.env", …]` and not
 * `[{ name, source, agent_id }]`. Written as `Vec<EnvFileRef>` first,
 * which is what the field looks like it should be; reading the branch is
 * what said otherwise.
 */
sourced_files: Array<string>, error?: string | null, };

// ── Operations ──

/** The payload a caller sends. */
export type SessionEnvQueryCall = { session_id: string, };

/** The payload the provider answers with. */
export type SessionEnvQueryReply = { 
/**
 * **Names**, not references — the handler maps the agent's array through
 * `as_str`, so this wire carries `["staging.env", …]` and not
 * `[{ name, source, agent_id }]`. Written as `Vec<EnvFileRef>` first,
 * which is what the field looks like it should be; reading the branch is
 * what said otherwise.
 */
sourced_files: Array<string>, error?: string | null, };

