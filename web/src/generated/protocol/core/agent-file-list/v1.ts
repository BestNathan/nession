// Generated by `just codegen` — do not edit.
//
// Source of truth: the Rust contracts in `crates/nession-git/src/protocol/`,
// `crates/nession-claude-code/src/protocol/` and `crates/nession-protocol/`.
// This file is agent.file.list / v1.

/** The canonical protocol id. Not the wire type. */
export const PROTOCOL = 'agent.file.list';
/** Every transport projection this contract travels as. */
export const WIRES = ['agent.file.list'] as const;
/**
 * The only projection this contract travels as.
 *
 * Absent, deliberately, on a contract served over more than one transport: it
 * has no single wire, and a caller that needs one has to say which it means.
 * `WIRES` is always there.
 */
export const WIRE = 'agent.file.list';
/** The contract version these shapes are. */
export const VERSION = 1;

// ── Shapes ──

export type FileListPayload = { path: string, };
export type FileListResponse = { entries: Array<FileEntry>, };
export type FileEntry = { name: string, path: string, 
/**
 * Absolute path on the filesystem, for actions like "copy full path".
 */
full_path: string, is_dir: boolean, size: number, modified: number, 
/**
 * Inferred MIME type (from file extension). Directories use "inode/directory".
 */
mime_type: string, 
/**
 * Whether the file is binary (non-text) content.
 */
is_binary: boolean, };

// ── Operations ──

/** The payload a caller sends. */
export type FileListCall = { path: string, };

/** The payload the provider answers with. */
export type FileListReply = { entries: Array<FileEntry>, };

