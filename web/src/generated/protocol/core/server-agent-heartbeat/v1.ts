// Generated by `just codegen` — do not edit.
//
// Source of truth: the Rust contracts in `crates/nession-git/src/protocol/`,
// `crates/nession-claude-code/src/protocol/` and `crates/nession-protocol/`.
// This file is server.agent.heartbeat / v1.

/** The canonical protocol id. Not the wire type. */
export const PROTOCOL = 'server.agent.heartbeat';
/** Every transport projection this contract travels as. */
export const WIRES = ['server.agent.heartbeat'] as const;
/**
 * The only projection this contract travels as.
 *
 * Absent, deliberately, on a contract served over more than one transport: it
 * has no single wire, and a caller that needs one has to say which it means.
 * `WIRES` is always there.
 */
export const WIRE = 'server.agent.heartbeat';
/** The contract version these shapes are. */
export const VERSION = 1;

// ── Shapes ──

export type AgentHeartbeatPayload = { agent_id: string, status: AgentStatus, session_count: number, active_sessions: number, metadata: HeartbeatMetadata, };
export type AgentStatus = "online" | "offline" | "degraded";
export type HeartbeatMetadata = { uptime_seconds: number, load_average: [number, number, number], 
/**
 * Agent version info — included in each heartbeat so the server
 * stays current after agent upgrades (previously only sent on register).
 */
agent: AgentMetadata | null, };
export type AgentMetadata = { tmux_version: string, os_version: string, nession_version: string, 
/**
 * Docker image tag (short sha) baked in at build time.
 * "dev" when running from `cargo run`, "unknown" when not set.
 */
image_tag: string, };

// ── Operations ──

/** The payload a caller sends. */
export type AgentHeartbeatCall = { agent_id: string, status: AgentStatus, session_count: number, active_sessions: number, metadata: HeartbeatMetadata, };

/**
 * No response alias: the catalog declares no response shape for this unit.
 */
