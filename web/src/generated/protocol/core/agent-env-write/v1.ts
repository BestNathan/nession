// Generated by `just codegen` — do not edit.
//
// Source of truth: the Rust contracts in `crates/nession-git/src/protocol/`,
// `crates/nession-claude-code/src/protocol/` and `crates/nession-protocol/`.
// This file is agent.env.write / v1.

/** The canonical protocol id. Not the wire type. */
export const PROTOCOL = 'agent.env.write';
/** Every transport projection this contract travels as. */
export const WIRES = ['agent.env.write'] as const;
/**
 * The only projection this contract travels as.
 *
 * Absent, deliberately, on a contract served over more than one transport: it
 * has no single wire, and a caller that needs one has to say which it means.
 * `WIRES` is always there.
 */
export const WIRE = 'agent.env.write';
/** The contract version these shapes are. */
export const VERSION = 1;

// ── Shapes ──

export type ClientEnvWritePayload = { name: string, source: EnvSource, agent_id?: string | null, content: string, 
/**
 * When false, refuse to overwrite an existing file (create-only).
 */
overwrite: boolean, };
export type ClientEnvWriteResponsePayload = { success: boolean, 
/**
 * True when the write was refused because the file already exists and
 * `overwrite` was false (UI prompts for confirmation).
 */
exists: boolean, error?: string | null, warnings: Array<string>, };
export type EnvSource = "server" | "agent";

// ── Operations ──

/** The payload a caller sends. */
export type EnvWriteCall = { name: string, source: EnvSource, agent_id?: string | null, content: string, 
/**
 * When false, refuse to overwrite an existing file (create-only).
 */
overwrite: boolean, };

/** The payload the provider answers with. */
export type EnvWriteReply = { success: boolean, 
/**
 * True when the write was refused because the file already exists and
 * `overwrite` was false (UI prompts for confirmation).
 */
exists: boolean, error?: string | null, warnings: Array<string>, };

