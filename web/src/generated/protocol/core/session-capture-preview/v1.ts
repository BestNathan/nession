// Generated by `just codegen` — do not edit.
//
// Source of truth: the Rust contracts in `crates/nession-git/src/protocol/`,
// `crates/nession-claude-code/src/protocol/` and `crates/nession-protocol/`.
// This file is session.capture-preview / v1.

/** The canonical protocol id. Not the wire type. */
export const PROTOCOL = 'session.capture-preview';
/** Every transport projection this contract travels as. */
export const WIRES = ['client.session.capture_preview', 'session.capture_preview'] as const;
/** The contract version these shapes are. */
export const VERSION = 1;

// ── Shapes ──

export type SessionCapturePreviewPayload = { session_name: string, lines: number, };
export type SessionCapturePreviewResponse = { ansi_b64: string, cols: number | null, rows: number | null, };

// ── Operations ──

/** The payload a caller sends. */
export type SessionCapturePreviewCall = { session_name: string, lines: number, };

/** The payload the provider answers with. */
export type SessionCapturePreviewReply = { ansi_b64: string, cols: number | null, rows: number | null, };

