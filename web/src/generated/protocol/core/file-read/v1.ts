// Generated by `just codegen` — do not edit.
//
// Source of truth: the Rust contracts in `crates/nession-git/src/protocol/`,
// `crates/nession-claude-code/src/protocol/` and `crates/nession-protocol/`.
// This file is file.read / v1.

/** The canonical protocol id. Not the wire type. */
export const PROTOCOL = 'file.read';
/** Every transport projection this contract travels as. */
export const WIRES = ['file.read'] as const;
/**
 * The only projection this contract travels as.
 *
 * Absent, deliberately, on a contract served over more than one transport: it
 * has no single wire, and a caller that needs one has to say which it means.
 * `WIRES` is always there.
 */
export const WIRE = 'file.read';
/** The contract version these shapes are. */
export const VERSION = 1;

// ── Shapes ──

export type FileReadPayload = { path: string, 
/**
 * Byte offset for chunked reads. `None` means start from beginning.
 *
 * `skip_serializing_if` as well as `default`, which is the rule this
 * directory follows for every skippable `Option`: serde reads a missing
 * key as `None` with `default` alone, but without the skip the field is
 * written as `null` — a *stated* absence where the contract says the key
 * may simply not be there.
 */
offset?: number | null, 
/**
 * Maximum bytes to return for chunked reads. `None` means use default chunk size.
 */
limit?: number | null, };
export type FileData = { path: string, 
/**
 * Base64-encoded file content.
 */
content: string, 
/**
 * MIME type: "text/plain", "application/json".
 */
mime_type: string, 
/**
 * Byte offset into the file where this chunk starts.
 */
offset: number, 
/**
 * Total file size in bytes.
 */
total_size: number, 
/**
 * Whether more bytes remain after this chunk.
 */
has_more: boolean, };

// ── Operations ──

/** The payload a caller sends. */
export type FileReadCall = { path: string, 
/**
 * Byte offset for chunked reads. `None` means start from beginning.
 *
 * `skip_serializing_if` as well as `default`, which is the rule this
 * directory follows for every skippable `Option`: serde reads a missing
 * key as `None` with `default` alone, but without the skip the field is
 * written as `null` — a *stated* absence where the contract says the key
 * may simply not be there.
 */
offset?: number | null, 
/**
 * Maximum bytes to return for chunked reads. `None` means use default chunk size.
 */
limit?: number | null, };

/** The payload the provider answers with. */
export type FileReadReply = { path: string, 
/**
 * Base64-encoded file content.
 */
content: string, 
/**
 * MIME type: "text/plain", "application/json".
 */
mime_type: string, 
/**
 * Byte offset into the file where this chunk starts.
 */
offset: number, 
/**
 * Total file size in bytes.
 */
total_size: number, 
/**
 * Whether more bytes remain after this chunk.
 */
has_more: boolean, };

