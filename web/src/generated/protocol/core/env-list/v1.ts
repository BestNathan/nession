// Generated by `just codegen` — do not edit.
//
// Source of truth: the Rust contracts in `crates/nession-git/src/protocol/`,
// `crates/nession-claude-code/src/protocol/` and `crates/nession-protocol/`.
// This file is env.list / v1.

/** The canonical protocol id. Not the wire type. */
export const PROTOCOL = 'env.list';
/** Every transport projection this contract travels as. */
export const WIRES = ['client.env.list', 'server.env.list'] as const;
/** The contract version these shapes are. */
export const VERSION = 1;

// ── Shapes ──

export type ClientEnvListPayload = Record<symbol, never>;
export type ClientEnvListResponsePayload = { files: Array<EnvFileInfo>, };
export type EnvFileInfo = { 
/**
 * Filename including the `.env` suffix (e.g. `staging.env`).
 */
name: string, 
/**
 * Where the file lives.
 */
source: EnvSource, 
/**
 * For `EnvSource::Agent`, the owning agent id. `None` for server files.
 */
agent_id?: string | null, 
/**
 * File size in bytes.
 */
size: number, 
/**
 * Last-modified time (unix seconds).
 */
modified: number, 
/**
 * Number of resolved variables (best-effort; excludes malformed lines).
 */
var_count: number, };
export type EnvSource = "server" | "agent";

// ── Operations ──

/** The payload a caller sends. */
export type EnvListCall = Record<symbol, never>;

/** The payload the provider answers with. */
export type EnvListReply = { files: Array<EnvFileInfo>, };

