// Generated by `just codegen` — do not edit.
//
// Source of truth: the Rust contracts in `crates/nession-git/src/protocol/`,
// `crates/nession-claude-code/src/protocol/` and `crates/nession-protocol/`.
// This file is git.diff / v1.

/** The canonical protocol id. Not the wire type. */
export const PROTOCOL = 'git.diff';
/** The transport projection this contract travels as. */
export const WIRE = 'extension.git.diff';
/** The contract version these shapes are. */
export const VERSION = 1;

// ── Shapes ──

export type DiffRequestV1 = { 
/**
 * Repository-relative. Validated agent-side before git runs — the client
 * naming an absolute path is refused there, not here, because a
 * client-side check would be a second and weaker boundary.
 */
path: string, session: string, };
export type DiffOkV1 = { diff: FileDiff, };
export type FileDiff = { path: string, 
/**
 * Unified diff text, already capped and lossily decoded for transport.
 */
text: string, 
/**
 * True when the file is binary — git reports that instead of a diff.
 */
binary: boolean, 
/**
 * Bytes dropped by the cap. Non-zero means `text` is a prefix, and the UI
 * must say so (#750 C3).
 */
truncated_bytes: number, truncated: boolean, };
export type SessionTargetV1 = { session: string, };

// ── Operations ──

/** The payload a caller sends. */
export type DiffRequest = { 
/**
 * Repository-relative. Validated agent-side before git runs — the client
 * naming an absolute path is refused there, not here, because a
 * client-side check would be a second and weaker boundary.
 */
path: string, session: string, };

/** The payload the provider answers with. */
export type DiffResponse = { "state": "ok", diff: FileDiff, } | { "state": "unavailable", reason: string, message: string, } | { "state": "not_a_repository", message: string, } | { "state": "error", message: string, };
