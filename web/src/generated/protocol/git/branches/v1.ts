// Generated by `just codegen` — do not edit.
//
// Source of truth: the Rust contracts in `crates/nession-git/src/protocol/`,
// `crates/nession-claude-code/src/protocol/` and `crates/nession-protocol/`.
// This file is git.branches / v1.

/** The canonical protocol id. Not the wire type. */
export const PROTOCOL = 'git.branches';
/** The transport projection this contract travels as. */
export const WIRE = 'extension.git.branches';
/** The contract version these shapes are. */
export const VERSION = 1;

// ── Shapes ──

export type BranchesRequestV1 = { 
/**
 * A request the agent clamps, on the same terms as `git.log`'s.
 */
limit?: number | null, session: string, };
export type BranchesOkV1 = { branches: Branches, };
export type Branches = { branches: Array<Branch>, 
/**
 * The count that was answered for, so the view can offer "more" honestly.
 */
limit: number, truncatedBytes: number, truncated: boolean, };
export type Branch = { name: string, 
/**
 * HEAD points here.
 */
current: boolean, 
/**
 * The configured upstream, `None` when the branch has none. Still set when
 * the upstream has been deleted — that is what `upstream_gone` reports.
 */
upstream?: string | null, 
/**
 * Commits this branch has that its upstream does not.
 */
ahead: number, 
/**
 * Commits the upstream has that this branch does not.
 */
behind: number, 
/**
 * The configured upstream no longer exists — git's `[gone]`.
 */
upstreamGone: boolean, };
export type SessionTargetV1 = { session: string, };

// ── Operations ──

/** The payload a caller sends. */
export type BranchesRequest = { 
/**
 * A request the agent clamps, on the same terms as `git.log`'s.
 */
limit?: number | null, session: string, };

/** The payload the provider answers with. */
export type BranchesResponse = { "state": "ok", branches: Branches, } | { "state": "unavailable", reason: string, message: string, } | { "state": "not_a_repository", message: string, } | { "state": "error", message: string, };
