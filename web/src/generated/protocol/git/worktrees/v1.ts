// Generated by `just codegen` — do not edit.
//
// Source of truth: the Rust contracts in `crates/nession-git/src/protocol/`,
// `crates/nession-claude-code/src/protocol/` and `crates/nession-protocol/`.
// This file is git.worktrees / v1.

/** The canonical protocol id. Not the wire type. */
export const PROTOCOL = 'git.worktrees';
/** Every transport projection this contract travels as. */
export const WIRES = ['git.worktrees'] as const;
/**
 * The only projection this contract travels as.
 *
 * Absent, deliberately, on a contract served over more than one transport: it
 * has no single wire, and a caller that needs one has to say which it means.
 * `WIRES` is always there.
 */
export const WIRE = 'git.worktrees';
/** The contract version these shapes are. */
export const VERSION = 1;

// ── Shapes ──

export type WorktreesRequestV1 = { session: string, };
export type WorktreesOkV1 = { worktrees: Worktrees, };
export type Worktrees = { worktrees: Array<Worktree>, truncatedBytes: number, truncated: boolean, };
export type Worktree = { 
/**
 * Absolute path as git records it.
 */
path: string, 
/**
 * The branch checked out there, without `refs/heads/`. `None` when the
 * entry is detached, bare, or has no `branch` line at all.
 */
branch?: string | null, 
/**
 * This is the work tree the Session is sitting in.
 */
current: boolean, detached: boolean, 
/**
 * A bare repository has no work tree; git lists the repository itself.
 */
bare: boolean, 
/**
 * `git worktree lock` reason, when it was locked. An empty string means
 * locked with no reason given, which is a different sentence from unlocked.
 */
locked?: string | null, 
/**
 * The directory is gone and the entry is waiting for `git worktree prune`.
 */
prunable?: string | null, };
export type SessionTargetV1 = { session: string, };

// ── Operations ──

/** The payload a caller sends. */
export type WorktreesRequest = { session: string, };

/** The payload the provider answers with. */
export type WorktreesResponse = { "state": "ok", worktrees: Worktrees, } | { "state": "unavailable", reason: string, message: string, } | { "state": "not_a_repository", message: string, } | { "state": "error", message: string, };

