// Generated by `just codegen` — do not edit.
//
// Source of truth: the Rust contracts in `crates/nession-git/src/protocol/`,
// `crates/nession-claude-code/src/protocol/` and `crates/nession-protocol/`.
// This file is git.status / v1.

/** The canonical protocol id. Not the wire type. */
export const PROTOCOL = 'git.status';
/** Every transport projection this contract travels as. */
export const WIRES = ['git.status'] as const;
/**
 * The only projection this contract travels as.
 *
 * Absent, deliberately, on a contract served over more than one transport: it
 * has no single wire, and a caller that needs one has to say which it means.
 * `WIRES` is always there.
 */
export const WIRE = 'git.status';
/** The contract version these shapes are. */
export const VERSION = 1;

// ── Shapes ──

export type StatusRequestV1 = { session: string, };
export type StatusOkV1 = { status: RepoStatus, root: string, 
/**
 * True when the listing itself was cut off. A partial listing presented as
 * a whole one is the failure `#750` C3 names.
 */
truncated: boolean, truncatedBytes: number, };
export type RepoStatus = { 
/**
 * Branch name, or `None` when detached.
 */
branch?: string | null, detached: boolean, upstream?: string | null, ahead: number, behind: number, 
/**
 * Tracked paths, Modified/Added/Deleted/Renamed — #750's "Modified" group.
 */
modified: Array<ChangedFile>, 
/**
 * Untracked paths. No diff is available for these, which is why the view
 * gives them no expander (#750 SC2).
 */
untracked: Array<string>, 
/**
 * Unmerged paths, present during a conflicted merge/rebase/cherry-pick.
 */
unmerged: Array<string>, };
export type ChangedFile = { path: string, 
/**
 * Present only for renames/copies.
 */
originalPath?: string | null, kind: ChangeKind, 
/**
 * Staged in the index (porcelain `X`).
 */
staged: boolean, 
/**
 * Changed in the working tree (porcelain `Y`).
 */
unstaged: boolean, };
export type ChangeKind = "modified" | "added" | "deleted" | "renamed" | "copied" | "typechanged" | "unmerged" | "unknown";
export type SessionTargetV1 = { session: string, };

// ── Operations ──

/** The payload a caller sends. */
export type StatusRequest = { session: string, };

/** The payload the provider answers with. */
export type StatusResponse = { "state": "ok", status: RepoStatus, root: string, 
/**
 * True when the listing itself was cut off. A partial listing presented as
 * a whole one is the failure `#750` C3 names.
 */
truncated: boolean, truncatedBytes: number, } | { "state": "unavailable", reason: string, message: string, } | { "state": "not_a_repository", message: string, } | { "state": "error", message: string, };

