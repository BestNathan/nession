// The grid web layout moved into the tools plugin (FilesWebLayout) in #561;
// this pattern name is kept as a re-export for tests and back-compat.
export { FilesWebLayout as FileWorkspace } from '@/session-first/workspace/tools/filesWeb';
