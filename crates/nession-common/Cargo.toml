[package]
name = "nession-common"
version.workspace = true
edition.workspace = true

[dependencies]
# Transition only (#678). The protocol DTOs currently in `src/protocol.rs` are
# being extracted into `nession-protocol`; until that finishes this crate
# re-exports the envelope from there so the definition has one home. The
# direction is one-way and stays that way: `nession-protocol` has no path back
# to this crate.
nession-protocol = { path = "../nession-protocol" }
anyhow.workspace = true
async-trait.workspace = true
serde.workspace = true
serde_json.workspace = true
chrono.workspace = true
uuid.workspace = true
thiserror.workspace = true
toml = "0.8"
dirs = "5"
libc = "0.2"
hostname.workspace = true
tracing.workspace = true
tracing-subscriber.workspace = true
tracing-appender.workspace = true

[lints]
workspace = true

[dev-dependencies]
tempfile = "3"
