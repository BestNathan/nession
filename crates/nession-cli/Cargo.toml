[package]
name = "nession-cli"
version.workspace = true
edition.workspace = true

[[bin]]
name = "nession"
path = "src/main.rs"

[dependencies]
nession-common = { path = "../nession-common" }
# The CLI is a protocol consumer, and the kernel is what a consumer reads.
nession-protocol = { path = "../nession-protocol" }
# How a protocol consumer speaks to a Server: transport, correlation, the
# handshake, one typed method per unit (#1015). Shared with the MCP host #565
# plans, which is why it is not in this crate.
nession-client = { path = "../nession-client" }
nession-agent = { path = "../nession-agent" }
nession-server = { path = "../nession-server" }
tokio.workspace = true
tokio-tungstenite.workspace = true
rustls.workspace = true
serde.workspace = true
serde_json.workspace = true
toml.workspace = true
tracing.workspace = true
tracing-subscriber.workspace = true
anyhow.workspace = true
chrono.workspace = true
futures-util.workspace = true
clap = { version = "4.4", features = ["derive", "env"] }
crossterm = "0.27"
nix = { version = "0.27", features = ["signal", "process"] }
libc = "0.2"
async-trait.workspace = true
base64.workspace = true
uuid.workspace = true
reqwest = { version = "0.12", default-features = false, features = ["rustls-tls", "stream", "json"] }
sha2 = "0.10"
flate2 = "1.0"
tar = "0.4"
semver = "1.0"
thiserror.workspace = true

[dev-dependencies]
tempfile = "3.10"
httptest = "0.16"

[lints]
workspace = true
